$(document).ready(function() {	
	/*select de localidades*/
	/*$('#selectProvincia').change(function () {
		$.ajax({
			type:'POST',
			url:'php/controllers/localidadSelectRecibiOfertas.controller.php',
			data: 'prov='+$(this).val(),
			success:function (ok) {
				$('#localidadContenedor').html(ok)
			}
		})
	})*/
	/* --- fin --- */
})